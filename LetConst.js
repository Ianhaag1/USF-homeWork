const PI = 3.14
PI = 42; //error

// you cannot redeclare a variable using Let. Let creates a 'block space'

// Same with Const

// you can reassign let but you cannot reassign const (constant)

// hoisting is using the compiler. Variables can be hoisted to the top of the scope or block space.